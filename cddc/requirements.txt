# Phase 1 + first real agent. Pinned majors; let patches float.
discord.py>=2.3,<3
python-dotenv>=1.0,<2
# DeepSeek + OpenAI/Codex are OpenAI-compatible; we use this SDK for both.
openai>=1.40,<2
# Claude provider uses the Anthropic SDK (only needed for CDDC_PROVIDER=claude).
anthropic>=0.40
# Harness worker (CDDC_WORKER=harness) drives the claude/codex CLIs in tmux from
# the host. Needs a `tmux` binary on the host too.
libtmux>=0.37,<1
